
## 2026-09-12 · correction · 財報要有獨立畫面，不只是詳情頁一行
- 命題標題就是「AI × 鑑識會計」，財報被縮成附錄一行後 Peter 要求「財報的部分要幫我調整一下」→ 選 A) 獨立「財務體檢」畫面（主線 6）。
- 規則：附錄定位（不進分數、不宣稱預測）可以保留，但**命題標題點名的東西要有自己的畫面**，否則評審看不到。

## 2026-09-12 · correction · 園名不要去識別
- Peter：「不要用去識別化，看不懂」。園所名稱是公開登記資料，代號讓承辦無法對照；改回真名。**只有負責人／行為人姓名**維持架構層不出（v_* view）。`anonymize_titles` 預設改 0。

## 2026-09-12 — kiang 主檔同名園兩筆 id（換委辦法人）
- **category**: knowledge_gap
- **what**: 非營利園換委辦法人後 kiang 會出現兩筆同園名不同 id（同 reg_date、皆 active），裁罰分散記在兩筆。用短名前綴 match 會隨機抓一筆 → 財報 preschool_id 對錯、被罰園數 9→10。
- **fix**: 財報封面第一頁有「新北市○○非營利幼兒園（委託△△辦理）」完整園名，`parse_statements.cover_title()` 抽出後以 title（括號全半形正規化）對 `preschools.title`。
- **rule**: 任何以園名對 kiang id 的地方，一律用完整 title，不用短名。

## 2026-09-12 — FY112 非營利園人事費科目搬動
- **category**: knowledge_gap
- **what**: 8/34 園 112 學年人事費率 0.32–0.44（他年 0.52–0.64），同年業務發展費、其他支出翻倍、總支出不變；兩份報告一致，非 OCR 錯。
- **rule**: 跨年比較用總支出 / 每名額支出；比人事費前先檢查前一年人事費率 ≥ 0.45（`finance.RECLASS_FLOOR`）。

## 2026-09-12 · correction · 裁罰事件不要只寫「第幾條」
- Peter：「裁罰事件 不要用第幾條，請說明具體違反什麼」。畫面上條號對承辦沒有意義；改用資料列自帶的違規描述，缺描述的列用該條在全國資料裡最常見的描述當標籤（`scripts/law_labels.py`，來源可追溯，不憑常識編）。熱圖與理由文字同步改「不當對待／超收／師生比」等。

## 2026-09-12 · correction · 排名表用 v1 mockup 的精簡版
- Peter 貼回 v1 mockup 的排名表（共 1,216 園、分數 0–100、等級、連坐代碼、最近裁罰、次數、核定、加入鈕、有標籤的下拉篩選、分頁）說「這理由比較好」。長句「理由」欄移到詳情頁與分數 tooltip；表格一列一行、所有園都列（無紀錄排最後）。
- 規則：資料表以「掃得快」為準——每列一行、數字靠右、理由收進 tooltip／詳情，不要在總表塞說明文字。

## 2026-09-12 · correction · 一輪實跑後的六條產品裁示
- 「你的分數應該要是一個模型吧」：使用者對「規則為主」的接受度低，分數要掛在模型上；解法不是硬上模型，而是把核准門檻改成可辯護的版本（單指標勝 0.02 且另一指標不落後），讓 AUC 真的比較好的邏輯迴歸過關，規則留作 fallback 並在模型頁寫清楚。
- 「字太小」：rivendell 14px 基準對政府承辦太小，這專案改 16px 基準、表格 15px。
- 「本季名單的等級高不夠用，要補分數與根因」：名單給稽查員用，每列要能直接變成到園的動作（根因＝違規類型計數、稽查重點三行）。
- 「負責人給名字」「不要用連坐」：詞彙要用承辦的詞；負責人是公開登記資料，可顯示。
- 「排程可以讓人選每區幾個跟 optima」：最佳化要有可調的目標與配額（參考 Verdandi-OR live console），不是黑盒一鍵。
- 「詳情第一眼要看到分數／機率／排名／裁罰數」：hero 放結論，關聯圖是佐證往下放。


## 2026-09-12 · best_practice · 對抗審查抓到的三類坑（Codex + Claude 雙聲道）
- **demo 資料與模型檔要一起進 repo**：demo DB 內有 active 模型但 pkl 被 gitignore，fresh clone 直接在評分階段中止。規則：任何「DB 指向檔案」的東西，bootstrap 要能重建或一起 commit，且缺檔要降級不中止。
- **核准與重新評分要同交易語意**：先切 active 再評分，評分失敗就留下壞狀態；改成 preflight 檔案存在 + 失敗自動撤回。
- **訓練先寫 DB 再寫檔、UPSERT 覆寫 active 列**：都會產生「DB 說有、檔案沒有」；改成先原子寫檔再 commit，且同鍵 active 列不覆寫。
